# xray-adapter

`xray-adapter` is the first runtime adapter name and compatibility target. Phase 0 + Phase 1 only defines the interface contract and placeholder module; no production traffic adapter is implemented yet.
