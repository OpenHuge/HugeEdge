# Helm Placeholder

Production Helm charts are intentionally deferred. This directory records the intended chart boundary for `api`, `worker`, `ext-runtime`, `web`, and optional agent update services.
