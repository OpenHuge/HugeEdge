# Terraform Placeholder

Production Terraform is intentionally out of scope for Phase 0 + Phase 1. Future modules should define cloud networking, managed Postgres, Redis, NATS, object storage, signing services, and runtime isolation boundaries.
