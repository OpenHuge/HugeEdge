openapi: 3.1.0
info:
  title: HugeEdge Extensibility API
  version: 0.1.0
paths:
  /v1/admin/capabilities:
    get:
      operationId: listCapabilityRegistry
      responses:
        "200":
          description: Capability registry
